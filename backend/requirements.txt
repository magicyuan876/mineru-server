# ============================================================================
# MinerU-Server Backend Requirements (Final Production Version)
# ============================================================================

# 1. 基础科学计算与底层支撑
numpy==1.26.4
pandas>=1.3.0
opencv-python-headless>=4.11.0.86
shapely>=2.0.0
pyclipper>=1.3.0
# [关键修复] 日志提示 paddlex 3.4.1 强依赖 PyYAML 6.0.2
PyYAML==6.0.2
psutil
py-cpuinfo
seaborn>=0.11.0
thop>=0.1.1
# [新增] 修复 FASTA 格式解析报错
biopython>=1.80

# 2. MinerU 3.0+ & VLM 必需依赖
# mineru[core] = pipeline + vlm + gradio，涵盖 pipeline/vlm-transformers/hybrid 后端。
# 不用 [all]：它在 Linux 上额外拉 mineru[vllm] -> vllm>=0.10.1.1，而本项目的 vLLM 是独立容器
# （vllm/vllm-openai 镜像）经 HTTP 调用，镜像内无需该包；且每个 vllm 版本都硬绑定特定 torch，
# 与本镜像钉死的 torch 2.10.0+cu130 冲突，会让 pip 回溯下载几十个 250MB+ 的 wheel。
mineru[core]>=3.0.9
# 以下为 mineru 依赖树中的关键包，显式声明以确保版本锁定
boto3>=1.34.0
botocore>=1.34.0
pypdf>=5.1.0
json-repair>=0.46.2
# fast-langdetect: mineru 3.0 仍约束 <0.3.0
fast-langdetect>=0.2.3,<0.3.0
magika>=0.6.2
pdftext>=0.6.3
beautifulsoup4>=4.14.0
scikit-image>=0.25.0
albumentations>=1.4.11
reportlab
pdfminer.six>=20251230
mineru-vl-utils>=0.1.19.1
qwen-vl-utils<1,>=0.0.14

# 3. Web 服务与 API 架构
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart>=0.0.20
litserve==0.2.16
aiohttp==3.11.11

# 4. 存储、工具库与系统交互
click>=8.1.7
requests>=2.32.5
loguru>=0.7.3
python-dotenv>=1.0.1
minio>=7.2.0
redis>=5.0.0
markitdown>=0.1.4
attrdict>=2.0.1
prettytable
ujson
chardet
ruamel.yaml
aistudio-sdk>=0.3.5
# [新增] 用于 Worker 控制 vLLM 容器启停
docker>=7.0.0

# 5. 模型管理与 OpenAI 兼容
modelscope>=1.34.0
huggingface-hub>=0.28.0,<1.0.0
openai>=1.50.0

# 6. 安全、认证与协议
pyjwt>=2.10.0
pydantic==2.10.5
pydantic-core==2.27.2
email-validator>=2.2.0
mcp==1.1.2
sse-starlette==2.2.1
jsonschema>=4.23.0
jsonschema-specifications>=2024.10.0

# 7. Python 兼容性与格式处理
setuptools>=75.0.0
packaging>=24.0
typing-extensions>=4.12.0
lxml>=6.0.0
regex>=2026.1.15
PyMuPDF>=1.25.0
Pillow>=11.0.0
img2pdf>=0.6.0
pikepdf>=10.0.0
matplotlib>=3.10.0
ftfy>=6.3.0

# 8. 音视频解析
funasr>=1.3.0
ffmpeg-python>=0.2.0
soundfile>=0.13.0
